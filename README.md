
  # Supplier Spot

  This is a code bundle for Supplier Spot. The original project is available at https://www.figma.com/design/s46zTZdUZocLOdUE5UZ8Nt/Supplier-Spot.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  